{
  "name": "replymint-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ReplyMint backend — turns screen context into one best reply via the Claude API.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch --env-file=.env src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "db:schema": "tsx --env-file=.env scripts/apply-schema.ts",
    "stt:smoke": "tsx --env-file=.env scripts/stt-smoke.ts",
    "eval": "tsx --env-file=.env scripts/eval/run.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "@hono/node-server": "^1.13.7",
    "@hono/node-ws": "^1.3.1",
    "google-auth-library": "^11.0.2",
    "hono": "^4.6.14",
    "postgres": "^3.4.9",
    "tsx": "^4.19.2",
    "ws": "^8.21.3",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.2"
  }
}
