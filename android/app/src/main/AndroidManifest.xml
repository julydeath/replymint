<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Draw the floating bubble on top of other apps. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Voice action (speech-to-text). -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- NOT for a persistent notification (we post none) — but Android/OEMs suppress an app's
         status toasts when its notifications are disabled, and ReplyMint's feedback rides on toasts. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Talk to the backend. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReplyMint">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- First-run tour + sign-in; MainActivity forwards here until setup is complete. -->
        <activity
            android:name=".ui.OnboardingActivity"
            android:exported="false" />


        <!-- The bubble + the only component that reads/writes other apps. Reads ON TAP ONLY;
             it also hosts the floating bubble (no foreground service, so no notification). -->

        <service
            android:name=".accessibility.ReplyMintAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
