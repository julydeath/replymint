org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Google Sign-In: the WEB OAuth client ID from Google Cloud Console (not the Android one).
# Not a secret — it ships inside the APK as the ID-token audience. Fill in after console setup;
# sign-in shows a clear error while this is empty. Must match the backend's GOOGLE_WEB_CLIENT_ID.
replymint.googleWebClientId=665655720291-9nhspuu42bk9qht3avm37dh7s87bp92f.apps.googleusercontent.com
